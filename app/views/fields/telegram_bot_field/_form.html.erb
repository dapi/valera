<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <% if field.data.present? %>
    <%= link_to field.display_text, field.telegram_url, target: '_blank', rel: 'noopener noreferrer', style: 'color: #2c5282; text-decoration: underline;' %>
  <% else %>
    <span class="text-muted">—</span>
  <% end %>
</div>
