/ Chat messages container with max-width and auto-scroll
/ Subscribe to Turbo Streams for real-time message updates in manager mode
/ Uses TenantChatsChannel for tenant-based authorization
= turbo_stream_from chat, channel: TenantChatsChannel

/ Messages are already ordered by controller (load_chat_with_messages)
div.mx-auto.space-y-4#chat-messages class="max-w-[800px]" data-controller="chat-scroll"
  - chat.messages.each do |message|
    = render 'message', message: message
