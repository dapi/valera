set -a
test -f .env.local && . .env.local
echo "User provider=${PROVIDER}"
. .env.${PROVIDER}
set +a

export BOT_TOKEN=$(pass show kuznik_auto_bot_token)

export ADMIN_DEV_URL=https://admin.${PORT}.brandymint.ru
export HOST=${PORT}.brandymint.ru
