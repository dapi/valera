# Operator membership for tenant one
operator_on_tenant_one:
  tenant: one
  user: operator_user
  role: 1

# Admin membership for tenant one
admin_on_tenant_one:
  tenant: one
  user: admin_member
  role: 2

# Viewer membership for tenant two
viewer_on_tenant_two:
  tenant: two
  user: viewer_user
  role: 0

# Viewer membership for tenant one (for testing)
viewer_on_tenant_one:
  tenant: one
  user: viewer_user_one
  role: 0
